<%= form_for(@micropost, html: { multipart: true }) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, :maxlength => 140, placeholder: "Write new Tweet...", id: "mp-content", style:"padding:10px;" %>
  </div>
  <%= f.submit "Post", class: "btn btn-twitter" %>
  <span class="picture">
    <%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>
  </span>
<% end %>

<script type="text/javascript">
  $('#micropost_picture').bind('change', function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    if (size_in_megabytes > 5) {
      alert('Maximum file size is 5MB. Please choose a smaller file.');
    }
  });
</script>
<script>
jQuery(document).ready(function($){
  
  function checkLength($edit, $maxlen) {
    if (parseInt($edit) > $maxlen) {
        $('#mp-content').css('border-color', 'red');
    }

  }  

  var $checker = document.getElementById('mp-content').innerHTML.length;
  checkLength(13, 14);

});
</script>